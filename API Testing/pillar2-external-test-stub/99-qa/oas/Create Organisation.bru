meta {
  name: Create Organisation
  type: http
  seq: 1
}

post {
  url: {{externalTestStubUrl}}/pillar2/test/organisation/{{testPlrId}}
  body: json
  auth: none
}

headers {

  Authorization: validBearerToken
}

body:json {
  {
    "orgDetails": {
      "domesticOnly": true,
      "organisationName": "Domestic Test Organisation Ltd",
      "registrationDate": "2024-01-01"
    },
    "accountingPeriod": {
      "startDate": "2024-01-01",
      "endDate": "2024-12-31"
    }
  }
}

tests {
  // Note: Returns 409 if organisation already exists
  test("Status code is 409 (already exists)", function() {
    expect(res.getStatus()).to.equal(409);
  });
}
