meta {
  name: Empty liable entities
  type: http
  seq: 1
}

put {
  url: {{apiUrl}}/uk-tax-return
  body: json
  auth: none
}

headers {
  X-Pillar2-Id: {{testPlrId}}
  Authorization: {{bearer_token}}
  Accept: application/vnd.hmrc.1.0+json
}

body:json {
  {
    "accountingPeriodFrom": "2024-01-01",
    "accountingPeriodTo": "2024-12-31",
    "obligationMTT": true,
    "electionUKGAAP": false,
    "liabilities": {
      "electionDTTSingleMember": false,
      "electionUTPRSingleMember": false,
      "numberSubGroupDTT": 1,
      "numberSubGroupUTPR": 1,
      "totalLiability": 5000,
      "totalLiabilityDTT": 5000,
      "totalLiabilityIIR": 0,
      "totalLiabilityUTPR": 0,
      "liableEntities": []
    }
  }
}

tests {
  test("should return 400 for empty liableEntities", function() {
    expect(res.getStatus()).to.equal(400);
  });

  test("should return INVALID_JSON error code", function() {
    expect(res.body.code).to.equal("INVALID_JSON");
  });

  test("should return invalid json message", function() {
    expect(res.body.message).to.equal("Invalid JSON payload");
  });
}
