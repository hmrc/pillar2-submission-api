meta {
  name: Valid request
  type: http
  seq: 3
}

put {
  url: {{apiUrl}}/overseas-return-notification
  body: json
  auth: none
}

headers {
  Authorization: {{bearer_token}}
  X-Pillar2-Id: {{testPlrId}}
  Accept: application/vnd.hmrc.1.0+json
}

body:json {
  {
    "accountingPeriodFrom": "2024-01-01",
    "accountingPeriodTo": "2024-12-31",
    "filedDateGIR": "2025-01-15",
    "countryGIR": "DE",
    "reportingEntityName": "Updated Entity Name",
    "TIN": "DE987654321",
    "issuingCountryTIN": "DE"
  }
}

tests {
  test("should return 200 OK", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("should contain processing date", function() {
    expect(res.body.processingDate).to.not.be.undefined;
  });
}
