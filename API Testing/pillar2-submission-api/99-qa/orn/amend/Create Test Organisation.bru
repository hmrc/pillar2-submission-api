meta {
  name: Create Test Organisation
  type: http
  seq: 1
}

post {
  url: {{apiUrl}}/setup/organisation
  body: json
  auth: none
}

headers {
  Authorization: {{bearer_token}}
  X-Pillar2-Id: {{testPlrId}}
}

body:json {
  {
    "orgDetails": {
      "domesticOnly": false,
      "organisationName": "Test Org ORN Amend",
      "registrationDate": "2022-01-01"
    },
    "accountingPeriod": {
      "startDate": "2024-01-01",
      "endDate": "2024-12-31"
    }
  }
}

tests {
  test("Status code is 201", function() {
    expect(res.getStatus()).to.equal(201);
  });
  
  test("Response contains organisation details", function() {
    expect(res.body.organisation.orgDetails.organisationName).to.equal("Test Org ORN Amend");
  });
}
