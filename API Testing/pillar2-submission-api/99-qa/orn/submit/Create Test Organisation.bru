meta {
  name: Create Test Organisation
  type: http
  seq: 1
}

post {
  url: {{externalTestStubUrl}}/pillar2/test/organisation/{{testPlrId}}
  body: json
  auth: none
}

headers {
  Authorization: validBearerToken
}

body:json {
  {
    "orgDetails": {
      "domesticOnly": false,
      "organisationName": "Test Org ORN Submit",
      "registrationDate": "2022-01-01"
    },
    "accountingPeriod": {
      "startDate": "2024-01-01",
      "endDate": "2024-12-31"
    },
    "lastUpdated": "2024-01-01T09:00:00Z"
  }
}

tests {
  test("Status code is 201", function() {
    expect(res.getStatus()).to.equal(201);
  });
  
  test("Response contains organisation details", function() {
    expect(res.body.organisation.orgDetails.organisationName).to.equal("Test Org ORN Submit");
  });
}

