meta {
  name: Missing query param (accountingPeriodTo)
  type: http
  seq: 5
}

get {
  url: {{apiUrl}}/overseas-return-notification?accountingPeriodFrom=2024-01-01
  body: none
  auth: none
}

params:query {
  accountingPeriodFrom: 2024-01-01
}

headers {
  Authorization: {{bearer_token}}
  X-Pillar2-Id: {{testPlrId}}
  Accept: application/vnd.hmrc.1.0+json
}

tests {
  test("should return 400 Bad Request", function() {
    expect(res.getStatus()).to.equal(400);
  });
  
  test("should contain correct error message", function() {
    expect(res.body.code).to.equal("BAD_REQUEST");
    expect(res.body.message).to.equal("Invalid request");
  });
}

